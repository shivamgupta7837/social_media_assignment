import 'package:flutter/material.dart';

class AppColors {
  
  static const Color background = Color(0xFF000000); 
  static const Color surface = Color(0xFF1C1C1E);    
  
  static const Color primaryAccent = Color(0xFF007AFF);   
  static const Color secondaryAccent = Color(0xFFFF2D55); 
  
  
  static const Color textPrimary = Colors.white;
  static Color textMuted = Colors.white.withOpacity(0.4);

  static Color glassWhite = Colors.white.withOpacity(0.1);
  static Color glassBorder = Colors.white.withOpacity(0.2);
}